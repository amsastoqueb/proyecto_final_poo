document.addEventListener("DOMContentLoaded", () => {
    const mapa = document.getElementById("mapa");
    if (mapa) {
        mapa.innerHTML = "<p style='text-align:center; padding-top:200px;'>Aquí se cargará el mapa interactivo</p>";
    }
});
